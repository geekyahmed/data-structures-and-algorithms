let names = ['john', 'matt', 'jospeh', 'john']
let newNames = ['jones', 'ola', 'ayo', 'tope']

//Concat two arrays
console.log(names.concat(newNames))

//Concat and sort
console.log(names.concat(newNames).sort())

